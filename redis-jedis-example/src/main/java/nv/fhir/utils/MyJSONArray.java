package nv.fhir.utils;



public class MyJSONArray {

/*

	JSONArray jsonarray;

	public MyJSONArray(JSONArray jsonarray) {
		this.jsonarray = jsonarray;
	}

	public String[] getStringArray() throws JSONException {
		String[] result = new String[jsonarray.length()];
		for (int i = 0; i < jsonarray.length(); i++)
			result[i] = jsonarray.getString(i);
		return result;
	}

	public int[] getIntArray() throws JSONException {
		int[] result = new int[jsonarray.length()];
		for (int i = 0; i < jsonarray.length(); i++)
			result[i] = jsonarray.getInt(i);
		return result;
	}

	public JSONObject[] getJSONObjectArray() throws JSONException {
		if (jsonarray.length() <= 0)
			return null;
		JSONObject[] result = new JSONObject[jsonarray.length()];
		for (int i = 0; i < jsonarray.length(); i++)
			result[i] = jsonarray.getJSONObject(i);
		return result;
	}
*/

}
